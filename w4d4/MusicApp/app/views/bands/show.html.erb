<h3><%= @band.name %></h3>

<ul>
  <% @albums.each do |album| %>
    <li><%= link_to "#{album.recordd}", album_url(album) %></li>
  <% end %>
</ul>
<br>
<%= link_to "Add Album", new_album_url %>
<br>
<%= button_to "Edit Band", edit_band_url, method: :get %>
<%= button_to "Delete", band_url(@band), method: :delete %>
<br>
<%= link_to "Bands", bands_url %>
